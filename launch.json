{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Game",
            "file": "${workspaceFolder}/index.html"
        }
    ]
} 